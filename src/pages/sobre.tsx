import React from 'react';

const Sobre = () => (
  <div
    style={{
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      height: '100vh',
      fontSize: '3rem'
    }}
  >
    Pagina em construção
  </div>
);

export default Sobre;
